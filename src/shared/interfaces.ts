export interface EmailProps {
  _id: string;
  created_at: string;
  email_address: string;
  full_name: string;
  message: string;
}

export interface ImageProps {
  width: string,
  height: string
}